export class Logger {
  // static isEnabled = window.location.href.includes('zetads_debug=true');

  // constructor(title) {
  //   this.title = {
  //     body: title || 'zetads',
  //     color: 'powderblue',
  //   };
  // }

  // log = (message, object='') => {
  //   if (Logger.isEnabled) {
  //     console.log(`%c${this.title.body}`, `background-color: ${this.title.color}; padding: 4px;`, message, object
  //     );
  //   }
  // }
}
